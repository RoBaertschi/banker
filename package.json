{
  "name": "banker",
  "scripts": {
    "dev": "bun run --watch src/index.tsx",
    "build": "bun build src/index.tsx --outdir ./build --splitting --target=bun"
  },
  "dependencies": {
    "dayjs": "^1.11.13",
    "hono": "^4.7.1",
    "mongodb": "^6.15.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "3.5.3",
    "typed-htmx": "^0.3.1"
  }
}
